import { Router } from 'express'

const router = Router()

router.get('/', (_req, res) => {
  res.json({
    message: 'Welcome to {{projectName}} API',
    version: '1.0.0'
  })
})

export { router as routes }
